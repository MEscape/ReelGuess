/**
 * Reactions feature — public API.
 *
 * Emoji reactions during the reveal phase.
 * Import from `@/features/reactions` instead of reaching into internal files.
 */

export { ReactionBar } from './components/reaction-bar'